# Heirring.com - Hire Smarter

Job portal built with React + TailwindCSS, Node.js + Express, and MongoDB.

## Quick start

```bash
# Backend
cd backend && npm install && npm run dev

# Frontend (from root)
npm install && npm run dev
```

See `backend/README.md` for API setup and `docs/` for testing guides.
